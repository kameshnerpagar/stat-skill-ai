# STAT-SKILL AI Environment Configuration

# Optional: OpenAI API Key for live LLM MCQ generation, StatBot responses, and quiz feedback.
# If omitted or left blank, the application automatically uses a deterministic smart fallback engine.
OPENAI_API_KEY=

# SQLite Database Location
DATABASE_URL=sqlite:///./statskill.db
